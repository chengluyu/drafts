#include <algorithm>
#include <iostream>
#include <vector>

using namespace std;

struct point {
  int x, y;
};

int abs(int x) {
  return x < 0 ? -x : x;
}

int main() {
  point pt;
  vector<int> vec;

  return 0;
}